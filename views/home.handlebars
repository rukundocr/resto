
<h3>USER ID </h3>

{{#if rfid}}
<div class="row">
    <div class="col s6">
        <h4 class="  green accent-4">{{rfid}}</h4>
        <a href="/user/" type="button" class="btn indigo ">BACK TO USERS</a>
    </div>
</div>
{{else}}

<p>No card available</p>

{{/if}}
<form>

<input type="text" name="card " value="{{rfid}}">


</form>